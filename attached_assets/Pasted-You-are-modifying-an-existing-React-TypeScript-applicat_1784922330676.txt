You are modifying an existing React + TypeScript application. Do not redesign, refactor, rename files, change routing, change styling, or modify any existing functionality unless it is required for the PDB viewer integration. Keep the current UI exactly as it is.

Goal

Replace the empty 3D viewer placeholder with a real interactive molecular viewer.

Requirements
Use Mol* (Molstar), the same viewer used by the RCSB Protein Data Bank.
Install any required npm dependencies.
Create a reusable ProteinViewer React component.
The component should accept a single prop:
pdbUrl: string
When pdbUrl changes, the viewer should automatically unload the previous structure and load the new one.
Display:
rotate
zoom
pan
residue selection
The viewer should completely fill the existing viewer card and resize responsively.
Existing Backend

The backend already returns:

{
  "structures": [
    {
      "id": "1M17",
      "title": "...",
      "method": "X-RAY DIFFRACTION",
      "download_url": "https://files.rcsb.org/download/1M17.pdb"
    }
  ]
}

Use the download_url.

Do not modify the backend.

Existing UI

The application already has:

Overview tab
Structure tab
Mutation tab
AI chat
Structure cards
Buttons that currently say things like:
Open PDB
View Structure
Open Viewer

Keep every existing card exactly the same.

Button behavior

Every existing "Open PDB" (or equivalent) button should simply do:

setSelectedPdb(structure.download_url)

The viewer should immediately load that structure.

No modal.

No new page.

No popup.

Load it inside the existing viewer section.

Initial State

If the search returns structures:

Automatically load the first structure into the viewer.

If there are no structures:

Display:

No experimental structure available.

inside the viewer card.

State

Use React state only.

Example:

const [selectedPdb, setSelectedPdb] = useState<string | null>(null)

Whenever a structure is clicked:

setSelectedPdb(structure.download_url)

The viewer should react automatically.

Do NOT

Do not redesign the UI.

Do not change spacing.

Do not change colors.

Do not replace Tailwind classes.

Do not modify any backend code.

Do not change any API endpoints.

Do not change existing tabs.

Do not touch the mutation workspace.

Do not modify AI chat.

Do not create a new page.

Do not rename components.

Only integrate Mol* into the existing 3D viewer area and connect the existing structure buttons to it.